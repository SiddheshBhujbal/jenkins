#include <stdio.h>
int main() {    

    int num1, num2, sum;
    printf("Enter two integers: ");
    scanf("%d %d", &numb1, &numb2);
    sum = numb1 + numb2;      
    printf("%d + %d = %d", num1, numb2, sum);
    return 0;
}

